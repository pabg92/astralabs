╔═══════════════════════════════════════════════════════════════════════════════╗
║                    CONTRACTBUDDY ARCHITECTURE DIAGRAM                         ║
╚═══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. USER UPLOADS CONTRACT                                                    │
└─────────────────────────────────────────────────────────────────────────────┘

    Browser                Next.js API              Supabase Storage
    ┌──────┐              ┌───────────┐            ┌──────────────┐
    │      │─── POST ────>│ /api/deals│            │   contracts  │
    │ User │   (PDF)      │           │───upload──>│    bucket    │
    │      │              │ route.ts  │            │  C36.pdf     │
    └──────┘              └─────┬─────┘            └──────────────┘
                                │
                                │ INSERT INTO
                                ↓
                    ┌────────────────────────┐
                    │  document_repository   │
                    ├────────────────────────┤
                    │ id: 05793c06...        │
                    │ deal_id: d3358f40...   │
                    │ status: "pending"      │
                    │ object_path: "..."     │
                    │ tenant_id: xxx         │
                    └────────┬───────────────┘
                             │
                             │ TRIGGER (automatic)
                             ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. DATABASE TRIGGER ENQUEUES FOR PROCESSING                                 │
└─────────────────────────────────────────────────────────────────────────────┘

              ┌─────────────────────────────────┐
              │ trigger_enqueue_document        │
              │ (migration 003)                 │
              └────────────┬────────────────────┘
                           │ calls
                           ↓
              ┌─────────────────────────────────┐
              │ enqueue_document_processing()   │
              │ (PL/pgSQL function)             │
              └────────────┬────────────────────┘
                           │ inserts message
                           ↓
              ┌─────────────────────────────────┐
              │   PGMQ Queue (PostgreSQL)       │
              │   document_processing_queue     │
              ├─────────────────────────────────┤
              │ Message: {                      │
              │   document_id: "05793c06...",   │
              │   tenant_id: "xxx",             │
              │   object_path: "tenant/.../pdf" │
              │ }                               │
              └────────────┬────────────────────┘
                           │
                           │ (message waits here)
                           ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. WORKER POLLS AND ORCHESTRATES (Hetzner LXC Container)                    │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────┐
    │  scripts/worker.ts (Background Process)            │
    │  Running 24/7 on your Hetzner server              │
    └────────────────────────────────────────────────────┘
              │
              │ (polls every 3 seconds)
              ↓
    ┌────────────────────────────────────────────────────┐
    │  while (true) {                                    │
    │    msg = dequeue_document_processing()             │
    │    if (msg) {                                      │
    │      await processDocument(msg)                    │
    │      delete_queue_message(msg.id)                  │
    │    }                                               │
    │    sleep(3000)                                     │
    │  }                                                 │
    └─────────────────┬──────────────────────────────────┘
                      │
                      │ processDocument() calls Edge Functions in sequence:
                      ↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. EDGE FUNCTION PIPELINE (Supabase Edge Functions - Deno Runtime)          │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌───────────────────────────────────────────────────────────────────────────┐
  │ STAGE 1: Extract Clauses (30-60 sec)                                      │
  └───────────────────────────────────────────────────────────────────────────┘

    Worker                  Edge Function              External APIs
    ┌──────┐               ┌──────────────┐           ┌────────────┐
    │      │─── POST ─────>│ extract-     │           │  OpenAI    │
    │      │   {doc_id}    │ clauses      │───────────>│  GPT-4o    │
    │      │               │              │   prompt   │            │
    │      │               │ - Downloads  │<───────────│ (analyzes  │
    │      │               │   PDF        │  response  │  contract) │
    │      │               │ - Extracts   │            └────────────┘
    │      │               │   text       │
    │      │               │ - Sends to   │
    │      │               │   OpenAI     │
    │      │               │ - Parses     │
    │      │<── SUCCESS ───│   clauses    │
    └──────┘               └──────┬───────┘
                                  │ INSERT INTO
                                  ↓
                    ┌─────────────────────────────┐
                    │   clause_boundaries         │
                    ├─────────────────────────────┤
                    │ id: uuid                    │
                    │ document_id: 05793c06...    │
                    │ clause_type: "Payment..."   │
                    │ content: "Client agrees..." │
                    │ start_page: 1               │
                    │ end_page: 2                 │
                    │ confidence: 0.95            │
                    │ tenant_id: xxx              │
                    └─────────────────────────────┘
                                  │
                                  │ UPDATE
                                  ↓
                    ┌─────────────────────────────┐
                    │  document_repository        │
                    │  status: "clauses_extracted"│
                    └─────────────────────────────┘

  ┌───────────────────────────────────────────────────────────────────────────┐
  │ STAGE 2: Generate Embeddings (10-20 sec)                                  │
  └───────────────────────────────────────────────────────────────────────────┘

    Worker                  Edge Function              External APIs
    ┌──────┐               ┌──────────────┐           ┌────────────┐
    │      │─── POST ─────>│ generate-    │           │  Cohere    │
    │      │   {doc_id}    │ embeddings   │───────────>│  Embed     │
    │      │               │              │  25 texts  │  API       │
    │      │               │ - Fetches    │<───────────│            │
    │      │               │   clauses    │  vectors   └────────────┘
    │      │               │ - Batch to   │
    │      │               │   Cohere     │
    │      │               │ - Store      │
    │      │<── SUCCESS ───│   vectors    │
    └──────┘               └──────┬───────┘
                                  │ INSERT INTO
                                  ↓
                    ┌─────────────────────────────┐
                    │   clause_embeddings         │
                    ├─────────────────────────────┤
                    │ id: uuid                    │
                    │ clause_boundary_id: xxx     │
                    │ embedding: [0.23, -0.45...] │
                    │ (1024 dimensions)           │
                    │ tenant_id: xxx              │
                    └─────────────┬───────────────┘
                                  │
                                  │ pgvector similarity search
                                  ↓
                    ┌─────────────────────────────┐
                    │   legal_clause_library      │
                    │   (shared source of truth)  │
                    ├─────────────────────────────┤
                    │ 300+ standard clauses       │
                    │ with embeddings             │
                    └─────────────┬───────────────┘
                                  │ find top 5 matches
                                  ↓
                    ┌─────────────────────────────┐
                    │   clause_match_results      │
                    ├─────────────────────────────┤
                    │ clause_boundary_id: xxx     │
                    │ matched_template_id: yyy    │
                    │ similarity_score: 0.87      │
                    └─────────────────────────────┘

  ┌───────────────────────────────────────────────────────────────────────────┐
  │ STAGE 3: Match & Reconcile (5-10 sec)                                     │
  └───────────────────────────────────────────────────────────────────────────┘

    Worker                  Edge Function              Database
    ┌──────┐               ┌──────────────┐           ┌────────────────┐
    │      │─── POST ─────>│ match-and-   │           │ pre_agreed_    │
    │      │   {doc_id}    │ reconcile    │──query───>│ terms          │
    │      │               │              │           │ (tenant-specific)
    │      │               │ - Fetch      │           └────────────────┘
    │      │               │   pre-agreed │
    │      │               │   terms      │           ┌────────────────┐
    │      │               │ - Compare    │──query───>│ clause_match_  │
    │      │               │   vs library │           │ results        │
    │      │               │ - Calculate  │           └────────────────┘
    │      │               │   RAG status │
    │      │               │ - Flag       │
    │      │<── SUCCESS ───│   issues     │
    └──────┘               └──────┬───────┘
                                  │
                                  │ UPDATE (multiple tables)
                                  ↓
            ┌───────────────────────────────────────────┐
            │  clause_match_results                     │
            ├───────────────────────────────────────────┤
            │  + rag_status: "green" | "amber" | "red"  │
            │  + rag_parsing: "acceptable"              │
            │  + rag_risk: "low"                        │
            │  + gpt_analysis: { reasoning: "..." }    │
            └───────────────────────────────────────────┘
                                  │
                                  ↓
            ┌───────────────────────────────────────────┐
            │  discrepancies (if issues found)          │
            ├───────────────────────────────────────────┤
            │  type: "missing_clause"                   │
            │  severity: "high"                         │
            │  description: "No IP clause found"        │
            └───────────────────────────────────────────┘
                                  │
                                  ↓
            ┌───────────────────────────────────────────┐
            │  document_repository                      │
            │  status: "completed" ✓                    │
            │  processed_at: timestamp                  │
            └───────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. USER VIEWS RESULTS                                                        │
└─────────────────────────────────────────────────────────────────────────────┘

    Browser                Next.js API              Supabase Database
    ┌──────┐              ┌────────────┐
    │      │── GET ──────>│ /api/      │           ┌──────────────────┐
    │ User │  ?dealId=... │ reconcil-  │──query───>│ document_repo    │
    │      │              │ iation/    │           │ clause_boundaries│
    │      │              │ [dealId]   │<─────────>│ match_results    │
    │      │<─ JSON ──────│            │           │ pre_agreed_terms │
    └──────┘  (clauses)   └────────────┘           └──────────────────┘
       │
       │ Renders on page:
       ↓
    ┌─────────────────────────────────────────┐
    │ Reconciliation Page                     │
    ├─────────────────────────────────────────┤
    │ ✓ Payment Terms  (Pages 1-2) • 95%     │
    │   [GREEN] Matches template              │
    │                                         │
    │ ⚠ IP Rights      (Pages 3-4) • 78%     │
    │   [AMBER] Needs review                  │
    │                                         │
    │ ✗ Termination    (Pages 5-6) • 45%     │
    │   [RED] Deviates from pre-agreed       │
    └─────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 6. USER COLLABORATION (Phase 11 - Your Recent PR)                           │
└─────────────────────────────────────────────────────────────────────────────┘

    Browser                Next.js API              Supabase Database
    ┌──────┐              ┌────────────┐
    │      │ [Suggest     │ /api/      │           ┌──────────────────┐
    │ User │  Change]     │ reconcil-  │──INSERT──>│ clause_redlines  │
    │      │─── POST ────>│ iation/    │           │                  │
    │      │  {proposed   │ [dealId]/  │           │ id: uuid         │
    │      │   text,      │ redlines   │           │ change_type: add │
    │      │   change     │            │           │ proposed_text    │
    │      │   type,      │            │           │ status: draft    │
    │      │   comment}   │            │           │ tenant_id: xxx   │
    │      │<─ SUCCESS ───│            │<─────────>│                  │
    │      │              │            │           │ clause_comments  │
    │      │              │            │           │                  │
    │      │ [View        │            │           │ id: uuid         │
    │      │  Comments]   │            │           │ comment_text     │
    │      │─── GET ─────>│            │           │ author_id        │
    │      │<─ JSON ──────│            │           │ tenant_id: xxx   │
    └──────┘              └────────────┘           └──────────────────┘

╔═══════════════════════════════════════════════════════════════════════════════╗
║ KEY COMPONENTS                                                                ║
╠═══════════════════════════════════════════════════════════════════════════════╣
║                                                                               ║
║  📁 Frontend (Next.js 15)                                                    ║
║     - app/reconciliation/page.tsx    : Main UI                              ║
║     - app/api/deals/route.ts         : Upload handler                       ║
║     - app/api/reconciliation/...     : Data fetching APIs                   ║
║                                                                               ║
║  ⚙️  Background Worker (Node.js) - RUNS ON YOUR HETZNER SERVER              ║
║     - scripts/worker.ts              : Polls queue, calls edge functions    ║
║     - Deployed as: systemd service in LXC container                         ║
║     - Runs 24/7, auto-restarts on failure                                   ║
║                                                                               ║
║  ☁️  Edge Functions (Deno on Supabase)                                      ║
║     - extract-clauses                : PDF → Text → Clauses (OpenAI)        ║
║     - generate-embeddings            : Clauses → Vectors (Cohere)           ║
║     - match-and-reconcile            : Compare & RAG assessment             ║
║                                                                               ║
║  🗄️  Database (PostgreSQL on Supabase)                                      ║
║     - document_repository            : Uploaded PDFs metadata               ║
║     - clause_boundaries              : Extracted clauses (per tenant)       ║
║     - clause_embeddings              : Vector embeddings                    ║
║     - legal_clause_library           : 300+ standard clauses (shared)       ║
║     - clause_match_results           : Similarity + RAG scores              ║
║     - pre_agreed_terms               : Tenant-specific expectations         ║
║     - clause_redlines                : Suggested changes (Phase 11)         ║
║     - clause_comments                : Discussion threads (Phase 11)        ║
║     - PGMQ Queue                     : Message queue for async processing   ║
║                                                                               ║
║  🔒 Multi-Tenancy                                                            ║
║     - Every tenant has isolated: deals, documents, clauses, terms           ║
║     - Shared library: All tenants use same legal clause library             ║
║     - Security: tenant_id on all queries (RLS in progress)                  ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝

╔═══════════════════════════════════════════════════════════════════════════════╗
║ DATA FLOW TIMING (for 1 document)                                            ║
╠═══════════════════════════════════════════════════════════════════════════════╣
║                                                                               ║
║  0:00  │ User uploads PDF                              [instant]            ║
║  0:01  │ Trigger enqueues to PGMQ                      [<1 sec]             ║
║  0:01  │ Worker polls and dequeues                     [3 sec polling]      ║
║  0:04  │ extract-clauses starts                        [30-60 sec]          ║
║  1:04  │ generate-embeddings starts                    [10-20 sec]          ║
║  1:24  │ match-and-reconcile starts                    [5-10 sec]           ║
║  1:34  │ Status: "completed" ✓                                             ║
║  1:35  │ User refreshes page → sees real data                              ║
║                                                                               ║
║  Total: ~90 seconds from upload to completion                               ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════════
TL;DR SUMMARY
═══════════════════════════════════════════════════════════════════════════════

1. User uploads PDF → Stored in Supabase Storage
2. Database trigger → Enqueues message to PGMQ
3. Worker on your Hetzner server → Polls queue every 3 seconds
4. Worker calls 3 Edge Functions sequentially → Process document
5. Edge Functions → Extract, embed, match clauses
6. Results stored in database with RAG status (green/amber/red)
7. User views reconciliation page → Fetches processed data
8. User suggests changes → Redlines/comments stored (Phase 11)

THE FIX FOR YOUR ISSUE: Start the worker on your Hetzner server!

═══════════════════════════════════════════════════════════════════════════════
